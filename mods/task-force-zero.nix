# mods/task-force-zero.nix — [TF] Task Force Zero
# https://steamcommunity.com/sharedfiles/filedetails/?id=3584051524
# Official mod of the TF servers: reworked vanilla layers, gameplay
# rebalancing and creative design changes. Small (~69 MB).
#
# Layer names unknown (not in SquadLayerList) — harvest in-game with
# `ListLayers` after enabling, then fill the list below. Its layers
# rework vanilla ones, so expect vanilla-like names with a TF prefix.
{
  workshopIds.task-force-zero = "3584051524";

  layers = [
    # "TF_Yehorivka_RAAS_v1"   # <- examples only; verify via ListLayers
  ];
}
